---
deployment:
  tasks:
    - export DEPLOYPATH=/home/username/laksamana-app
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH/server && /usr/local/bin/npm install --production
    - cd $DEPLOYPATH/client && /usr/local/bin/npm install && /usr/local/bin/npm run build
